// const EqProfile *eq_profile_get(EqPresetID id);
// const char *eq_profile_name(EqPresetID id);

#include <stdio.h>
#include <stdlib.h>
#include "eq-profiles.h"

const EqProfile *eq_profile_get(EqPresetID id) {
    // should get the values of the eq profile based on the inputted id
}

const char *eq_profile_name(EqPresetID id) {
    // set name of an eq profile
}